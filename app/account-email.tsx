'use client';
import {useState} from 'react';
export default function AccountEmail({currentEmail}:{currentEmail?:string|null}){
 const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState(false);
 return <section className="email-panel"><h3>E-mail de acesso e recuperação</h3><p>{currentEmail?`Confirmado: ${currentEmail}`:'Cadastre um e-mail para entrar e recuperar sua senha.'}</p><p className="support left">Prefira seu e-mail da rede estadual, escola ou empresa. E-mails pessoais também são aceitos. Use um endereço individual ao qual você tenha acesso.</p><form className="form" onSubmit={async e=>{e.preventDefault();if(busy)return;setBusy(true);setMessage('');setError(false);try{const r=await fetch('/api/auth',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'setEmail',email,password})});const d:any=await r.json();if(!r.ok)throw new Error(d.error);setMessage(d.message);setPassword('')}catch(e:any){setError(true);setMessage(e.message||'Não foi possível enviar. Tente novamente.')}finally{setBusy(false)}}}><label htmlFor="account-email">{currentEmail?'Novo e-mail':'Seu e-mail'}</label><input id="account-email" type="email" autoComplete="email" required maxLength={254} value={email} onChange={e=>setEmail(e.target.value)}/><label htmlFor="account-email-password">Senha atual do jogo</label><input id="account-email-password" type="password" autoComplete="current-password" required maxLength={128} value={password} onChange={e=>setPassword(e.target.value)}/><button className="secondary" disabled={busy}>{busy?'Enviando…':'Enviar confirmação'}</button></form>{message&&<p role={error?'alert':'status'}>{message}</p>}<p className="support left">O endereço só será vinculado após a confirmação. Isso não altera sua aprovação nem seu tipo de conta.</p></section>
}
