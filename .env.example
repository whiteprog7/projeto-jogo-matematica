# Server-only. Configure real values in the hosting environment, never in Git.
ADMIN_PROFILE_ID=
AUTH_PEPPER=
ADMIN_PASSWORD_HASH=
# Required to activate verification and recovery email delivery.
APP_ORIGIN=
RESEND_API_KEY=
EMAIL_FROM=
